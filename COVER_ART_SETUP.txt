╔══════════════════════════════════════════════════════════════════╗
║              COVER ART - SETUP & DEPLOYMENT GUIDE                ║
║                   Production Ready ✅                             ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│ STEP 1: ADD ENVIRONMENT VARIABLES TO NETLIFY (REQUIRED)       │
└────────────────────────────────────────────────────────────────┘

Go to: Netlify Dashboard → Site Settings → Environment Variables

Add these 2 required variables:

1. OPENAI_API_KEY
   Value: sk-proj-YOUR_KEY_HERE
   Get from: https://platform.openai.com/api-keys

2. SUPABASE_SERVICE_ROLE_KEY
   Value: eyJhbGc...YOUR_SERVICE_KEY
   Get from: Supabase Dashboard → Project Settings → API → service_role

Optional (already set):

3. SUPABASE_COVER_BUCKET
   Value: artist-assets

4. VITE_DEV_OVERRIDE_EMAILS
   Value: milesdorre5@gmail.com

┌────────────────────────────────────────────────────────────────┐
│ STEP 2: CREATE STORAGE BUCKET IN SUPABASE                     │
└────────────────────────────────────────────────────────────────┘

Go to: Supabase Dashboard → Storage → Buckets

1. Create new bucket:
   - Name: artist-assets
   - Public: ✅ Yes

2. Run this SQL (SQL Editor):

CREATE POLICY "Public read access"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'artist-assets');

CREATE POLICY "Service role insert"
ON storage.objects FOR INSERT
TO service_role
WITH CHECK (bucket_id = 'artist-assets');

┌────────────────────────────────────────────────────────────────┐
│ STEP 3: DEPLOY TO NETLIFY                                     │
└────────────────────────────────────────────────────────────────┘

Option A: Push to GitHub (auto-deploys)
  git add .
  git commit -m "Fix: Cover Art server-side upload"
  git push origin main

Option B: Manual deploy (if Netlify CLI installed)
  npm run deploy

Wait ~2 minutes for build completion

┌────────────────────────────────────────────────────────────────┐
│ STEP 4: TEST IT WORKS                                         │
└────────────────────────────────────────────────────────────────┘

1. Login as: milesdorre5@gmail.com
2. Go to: Studio → Cover Art
3. Verify: Shows "∞ DEV" badge (top right)
4. Generate: Enter any prompt
5. Check Network tab:
   - Request: /.netlify/functions/generate-cover-art
   - Status: 200 OK
   - Response: { "publicUrl": "...", "remainingCredits": ... }
6. Image displays immediately ✅

┌────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                │
└────────────────────────────────────────────────────────────────┘

❌ 500 Error → Check Netlify Function Logs

Common errors:
- "Missing OPENAI_API_KEY" → Add key to Netlify env
- "Missing Supabase credentials" → Add service role key
- "storage_upload_failed" → Create artist-assets bucket

❌ 402 Error (INSUFFICIENT_CREDITS) → Normal for non-dev users
   Fix: Login as milesdorre5@gmail.com (has override)

❌ Image doesn't load → Check Supabase Storage
   - Bucket: artist-assets
   - Public: Yes
   - RLS policies: Set (see Step 2)

┌────────────────────────────────────────────────────────────────┐
│ HOW IT WORKS                                                   │
└────────────────────────────────────────────────────────────────┘

Browser → Netlify Function → OpenAI → Supabase Storage → Browser

1. Frontend calls /.netlify/functions/generate-cover-art
2. Function checks credits (or dev override)
3. Function generates image via OpenAI DALL-E 3
4. Function uploads to Supabase Storage (server-side)
5. Function returns publicUrl
6. Frontend displays image immediately

Benefits:
✅ No CORS issues
✅ No CSP issues  
✅ API keys never exposed
✅ Server-side upload (secure)
✅ Credit deduction in one transaction

┌────────────────────────────────────────────────────────────────┐
│ DEV OVERRIDE (milesdorre5@gmail.com)                          │
└────────────────────────────────────────────────────────────────┘

Features:
✅ Unlimited generations
✅ No credit deduction
✅ Shows "∞ DEV" badge
✅ Always enabled (no credit check)

Verify in logs:
[generate-cover-art] User: milesdorre5@gmail.com, Dev Override: true
[generate-cover-art] Dev override - credits not deducted

┌────────────────────────────────────────────────────────────────┐
│ COST PER GENERATION                                            │
└────────────────────────────────────────────────────────────────┘

OpenAI DALL-E 3: ~$0.04 USD per 1024x1024 image
Supabase Storage: ~$0.021/GB stored
Netlify Function: FREE (included)

In-app cost:
- Standard: 300 Tools credits
- Upscale: 150 Tools credits  
- Dev Override: FREE

┌────────────────────────────────────────────────────────────────┐
│ FILES MODIFIED                                                 │
└────────────────────────────────────────────────────────────────┘

✅ netlify/functions/generate-cover-art.ts
✅ src/components/CoverArtGenerator.tsx
✅ public/_headers (CSP fonts)
✅ src/lib/devWalletOverride.ts
✅ .env (added OPENAI_API_KEY placeholder)

┌────────────────────────────────────────────────────────────────┐
│ SUCCESS INDICATORS                                             │
└────────────────────────────────────────────────────────────────┘

✅ Function deployed (Netlify → Functions → generate-cover-art)
✅ No env var errors in function logs
✅ Network shows 200 OK response
✅ Image displays in browser
✅ Files appear in Supabase Storage
✅ Credits deduct correctly (non-dev users)
✅ Miles sees ∞ DEV badge

┌────────────────────────────────────────────────────────────────┐
│ DOCUMENTATION                                                  │
└────────────────────────────────────────────────────────────────┘

📄 COVER_ART_QUICK_FIX.md - Start here
📄 COVER_ART_SERVER_SIDE_COMPLETE.md - Full technical docs
📄 COVER_ART_TROUBLESHOOTING.md - Debug guide
📄 COVER_ART_IMPLEMENTATION_SUMMARY.md - Overview

═══════════════════════════════════════════════════════════════════
                 Ready to deploy! 🚀
═══════════════════════════════════════════════════════════════════
